# Discovery scripts (non-production)

Throwaway probes used while writing `docs/live-transfer-discovery.md`. Safe to delete after review; not part of the product build.

```bash
node scripts/discovery/probe-fpl-api.mjs > scripts/discovery/probe-results.json
```
